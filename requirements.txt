# Core API framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# Document processing
pypdf>=5.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
pandas>=2.2.0

# OCR support - multiple engine options
# Legacy OCR (requires Tesseract installed on system)
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0

# VLM-based OCR engines (optional - install based on preference)
# Primary: Marker (uses Surya, best accuracy/speed balance)
# pip install marker-pdf
# Fallback: Docling (IBM, MIT licensed, CPU-friendly)
docling>=2.0.0

# Embeddings and vector search
sentence-transformers>=3.0.0
faiss-cpu
numpy<2.0.0

# AI providers (optional - users provide their own API keys)
anthropic>=0.40.0
openai>=1.0.0
httpx>=0.28.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Testing
pytest>=8.0.0
