# Core API framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18

# Document processing
pypdf==5.1.0                   # PDF extraction
pdfplumber==0.11.4             # PDF extraction (complex layouts)
python-docx==1.1.2             # DOCX extraction
pandas==2.2.3                  # CSV extraction

# v3.0: OCR support for scanned PDFs (optional)
# Install with: pip install pytesseract pdf2image pillow
# Also requires system Tesseract installation:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   macOS: brew install tesseract
#   Linux: apt-get install tesseract-ocr
pytesseract>=0.3.10            # OCR engine wrapper (optional)
pdf2image>=1.16.0              # PDF to image conversion (optional)
Pillow>=10.0.0                 # Image processing for OCR

# Alternative OCR engine (optional, GPU-accelerated)
# Install with: pip install easyocr
# easyocr>=1.7.0               # EasyOCR for better accuracy (optional)

# Embeddings and similarity search
sentence-transformers==3.3.1
faiss-cpu

# AI enhancements (optional - users provide their own API keys)
anthropic>=0.40.0
openai>=1.0.0

# Utilities
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Development and testing
pytest==8.3.4
httpx==0.28.1
